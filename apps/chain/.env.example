# Only needed for `pnpm deploy:amoy` — local Hardhat dev/test needs nothing.
CHAIN_RPC_URL=""     # e.g. an Alchemy/Infura Polygon Amoy endpoint
CHAIN_PRIVATE_KEY=""  # funded testnet wallet — never a mainnet key
